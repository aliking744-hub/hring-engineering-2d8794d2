# گیت عرضه عمومی HRing

این سند اجرای `prgold` را قابل‌پیگیری می‌کند. تا زمانی که همه گیت‌ها با شاهد واقعی پاس نشده‌اند، پیام عمومی محصول فقط «نسخه آزمایشی فعال است؛ برای تست ثبت‌نام کنید» است.

## گیت ۱ — قابلیت‌های وعده‌داده‌شده
- [x] Smart Ad، پروفایل شغلی، Interview Kit، Onboarding، Salary و Dashboard در CI پوشش دارند.
- [x] Learning Path و Labor Complaint دارای قرارداد JSON پایدار و مسیر native هستند.
- [ ] Legal Defense باید به job غیرهمزمان با وضعیت، progress، timeout و refund قابل‌پیگیری تبدیل شود.
- [ ] برای هر قابلیت، یک UAT واقعی production با دادهٔ ساختگی و ثبت هزینه انجام شود.

## گیت ۲ — خروجی قابل‌اعتماد
- [x] اطلاعات تماس پشتیبانی فقط از CMS خوانده می‌شود.
- [x] Legal Advisor در نبود منبع، پاسخ مستند جعلی تولید نمی‌کند.
- [x] نام شرکت در Smart Ad بدون بازنویسی مدل حفظ می‌شود.
- [x] placeholderهای Onboarding از دادهٔ واقعی فرم پر می‌شوند.
- [x] تصویر Smart Ad در MinIO ذخیره، در history خصوصی فهرست و فقط با دانلود مالک از URL خصوصی قابل بازیابی است.

## گیت ۳ — بسته و اعتبار
- [x] rate card سروری و نمایش هزینهٔ عملیات‌های اصلاح‌شده وجود دارد.
- [ ] نرخ/محدودیت بسته‌های فردی و سازمانی باید توسط مالک محصول تأیید و در پنل اعمال شود.
- [ ] سناریوی پایان اعتبار و پایان دورهٔ اشتراک باید UAT شود.

## گیت ۴ — خرید تا تحویل
- [ ] درگاه پرداخت تأییدشده، webhook و دریافت شناسهٔ تراکنش لازم است.
- [ ] تست خرید sandbox/واقعی: پرداخت، فاکتور، فعال‌سازی، کسر اعتبار، تمدید و refund.
- [ ] در صورت خطا، هیچ اعتبار یا اشتراک نیمه‌کاره نباید باقی بماند.

## گیت ۵ — امنیت
- [ ] آزمون امنیت مجاز روی حساب، سازمان، ادمین، فایل و API با گزارش بازتولیدپذیر.
- [ ] رفع موارد اولویت‌دار `prsec` و بازبینی پس از رفع.

## گیت ۶ — سفر مشتری
- [ ] UAT موبایل و دسکتاپ: ثبت‌نام ← خرید ← استفاده ← history ← پشتیبانی.
- [ ] بررسی RTL، دسترس‌پذیری، خطای قابل‌فهم و refresh/idempotency.

## گیت ۷ — ملزومات فروش
- [ ] متن حقوقی تأییدشده: شرایط استفاده، حریم خصوصی و بازگشت وجه.
- [ ] راه تماس مالک/پشتیبانی در CMS تأیید شود.
- [ ] Search Console و baseline سرعت ثبت شود.

## خروج از گیت
برای هر مربع باز باید تاریخ، محیط، شناسه تست/تراکنش و نتیجه ثبت شود. CI سبز به‌تنهایی شاهد خروج از گیت نیست.


## backlog یکپارچهٔ پیش از فروش (prprice + prisue + prsec + UAT میزکار)

### P0 — مانع عرضه
- [ ] خروجی همهٔ ماژول‌ها: PDF دانلود واقعی، لوگو/فونت/RTL یکسان، history و عدم بازشدن tab بی‌فایده.
- [ ] Learning Path: تشخیص پاسخ ناقص، پیام قابل‌فهم، و UAT بدون کسر اعتبار در خطا.
- [x] Smart Ad: پس‌زمینهٔ تولیدی در MinIO خصوصی ذخیره می‌شود؛ کامپوزیت نهاییِ متن/لوگو در مرورگر روی همان دارایی خصوصی جایگزین می‌شود، history مالک و دانلود auth‌شده دارد و پاسخ API به‌جای Base64 حجیم URL خصوصی برمی‌گرداند.
- [ ] آزمون امنیت مجاز و بازتولیدپذیر: auth/RBAC/IDOR، file upload، API rate limit و dependency/secret scan.
  - [x] dependency audit فرانت‌اند و هر دو سرویس پایتون در CI اجباری است؛ وابستگی‌های دارای CVE شناخته‌شده به نسخهٔ patched ارتقا یافتند.
  - [ ] secret scan و آزمون مجاز staging هنوز شاهد مستقل لازم دارد.

### P1 — کیفیت محصول
- [ ] Job Profile: کاتالوگ نسخه‌دارِ نقش‌ها با مرجع O*NET و لایهٔ ایرانیزه؛ تولید مدل فقط در چارچوب کاتالوگ.
- [x] Interview Kit: دقیقاً ۱۱ سؤال و تمرکز اصلی ۵ سؤال دارد؛ PDF برندشده باید UAT شود.
- [ ] Onboarding: تاریخ یک‌دست شمسی، breakdown هفته‌ای، خروجی قابل‌تحویل و ایمیل خوش‌آمد.
- [ ] نقشهٔ ۹۰ روزه: ریزتسک، وضعیت جزئی، گروه تکمیل‌شده، گواهی با امتیاز و تاریخ‌های هم‌تقویم.
- [ ] Dashboard: شارژ idempotent دمو، نرخ‌گذاری براساس ردیف import و گزارش PDF یکسان.
- [x] Salary: حق مسکن و بن خواروبار قابل‌ویرایش برای مبلغ واقعی شرکت است؛ گزارش مدیریتی و مشخصات فردی باقی است.
- [ ] Profiles: اتصال به سازمان/نقش/history یا حذف از مسیر کاربر تا زمان تکمیل.
- [ ] Legal: corpus نسخه‌دار از منابع رسمی، citation ماده/تاریخ، و تحلیل ریسک به‌جای وعدهٔ «احتمال برد» قطعی.

### وابستگی‌های خارج از کد
- [ ] پرداخت: درگاه، webhook، transaction UAT و refund تنها پس از آماده‌شدن درگاه انجام می‌شود.
- [ ] متن نهایی شرایط استفاده، حریم خصوصی و بازگشت وجه باید توسط مالک/مشاور حقوقی تأیید شود.
